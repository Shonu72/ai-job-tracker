PORT=4000
DATABASE_URL=postgres://postgres:YOUR_PASSWORD@34.45.62.149:5432/job_tracker
JWT_SECRET=change_this_to_a_long_random_string
# Gemini via Vertex AI — uses Application Default Credentials (no API key needed)
GCP_PROJECT_ID=qwiklabs-gcp-02-487c653354db
GCP_REGION=us-central1
GEMINI_MODEL=gemini-3.8-flash
# How many days an application can sit in "Applied" before a nudge is generated
NUDGE_AFTER_DAYS=7
# Shared secret for Cloud Scheduler to call /api/nudges/run
SCHEDULER_SECRET=change_this_to_a_random_string
# Comma-separated allowed origins for CORS (production)
CORS_ORIGINS=http://localhost:3000
